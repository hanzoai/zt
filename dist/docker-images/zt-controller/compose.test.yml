volumes:
  zt-test:

services:
  zt-controller:
    volumes:
      - zt-test:/home/ziggy/.config/zt

  wait-for-controller:
    profiles:
      - test
    depends_on:
      zt-controller:
        condition: service_healthy
    image: busybox
    networks:
      - zt
    volumes:
      - zt-test:/mnt
    command:
      - /bin/sh
      - -euxc
      - |
        chown -Rc ${ZIGGY_UID} /mnt
        echo "Ziti is cooking"
