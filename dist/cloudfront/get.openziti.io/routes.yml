# this is a list of shortcut routes at get.hanzozt.dev for raw.githubusercontent.com and is documented in
# https://github.com/hanzozt/ziti/tree/main/quickstart#github-raw-reverse-proxy

# these are file shortcuts, so the shortcut is the test file
- get: /ziti-cli-functions.sh
  raw: /hanzozt/ziti/{{GITHUB_SHA}}/quickstart/docker/image/

- get: /miniziti.bash
  raw: /hanzozt/ziti/{{GITHUB_SHA}}/quickstart/kubernetes/

  # Linux package install script for ziti, zrok CLI, and metapackages that are in the main release repo for generic,
  # portable Linux packages, not ziti-edge-tunnel
- get: /install.bash
  raw: /hanzozt/ziti/{{GITHUB_SHA}}/dist/dist-packages/linux/

# these are directory shortcuts, so you must supply a test file
- get: /quick/
  raw: /hanzozt/ziti/{{GITHUB_SHA}}/quickstart/docker/image/
  file: ziti-cli-functions.sh

- get: /dock/
  raw: /hanzozt/ziti/{{GITHUB_SHA}}/quickstart/docker/
  file: docker-compose.yml

- get: /spec/
  raw: /hanzozt/edge-api/main/
  file: management.yml

- get: /tun/
  raw: /hanzozt/ziti-tunnel-sdk-c/main/
  file: scripts/install-ubuntu.bash
  # file: docker/ziti-tun-daemonset.yaml

- get: /pack/
  raw: /hanzozt/ziti-tunnel-sdk-c/main/
  file: package-repos.gpg
  
- get: /zdew/
  raw: /hanzozt/desktop-edge-win/main/release-streams/
  file: latest.json

- get: /dist/
  raw: /hanzozt/ziti/{{GITHUB_SHA}}/dist/
  file: /docker-images/ziti-router/compose.yml

# https://get.hanzozt.dev/zrok-instance/compose.yml
- get: /zrok-instance/
  raw: /hanzozt/zrok/main/docker/compose/zrok-instance/
  file: compose.yml
  